function goback(element) { element.style.animation = "forwards 0.5s smallerlink"; }
function goin(element) { element.style.animation = "forwards 0.5s biggerlink"; }
function bloom(element) { element.style.animation = "forwards 0.5s bloom"; }
function nobloom(element) { element.style.animation = "forwards 0.5s nobloom"; }
function round(element) { element.style.animation = "forwards 0.2s round"; }
function unround(element) { element.style.animation = "forwards 0.2s noround"; }